---
title: 'MATCH'
---


Find shortest path p, from a user u1 to user u2 traversing :knows edges 1+ (`{1,}`) times.


```
MATCH p = ANY SHORTEST (u1:users {username: "saqib"})-[:knows]->{1,}(t2:users {username: "uroosa"}) 
RETURN u1.username, u2.username, path_length(p) AS num_hops
```
